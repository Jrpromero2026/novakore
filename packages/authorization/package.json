{
  "name": "@novakore/authorization",
  "version": "0.0.0",
  "private": true,
  "description": "NovaKore server-side authorization: pure permission resolution over membership + role-assignment grants. Deny by default.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@novakore/domain": "*"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
