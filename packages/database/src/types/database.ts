/**
 * PLACEHOLDER — replaced by generated types once the Phase 1A schema lands.
 *
 * Generation command (documented in docs/development/supabase.md):
 *   npm run db:types
 *
 * Never hand-maintain row types that the generator produces.
 */
export type Database = Record<string, never>;
