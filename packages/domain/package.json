{
  "name": "@novakore/domain",
  "version": "0.0.0",
  "private": true,
  "description": "NovaKore canonical domain: terminology keys, versioned content-block schema registry, rule condition trees. Type-level prototypes and architecture tests only — no I/O.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
